import { Client } from "../client/Client";
import { IMessage } from "../client/IMessage";
export declare class Inbox {
    private readonly client;
    private readonly name;
    private self;
    private id;
    readonly ID: number;
    constructor(client: Client, name: string);
    waitForMessages(condition: (messages: IMessage[]) => boolean, messageFilter?: (message: IMessage) => boolean): Promise<void>;
    getMessages(messageFilter?: (message: IMessage) => boolean): Promise<IMessage[]>;
    deleteMessages(messageFilter?: (message: IMessage) => boolean): Promise<void>;
    private init;
}
